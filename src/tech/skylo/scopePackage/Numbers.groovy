package tech.skylo.scopePackage

import groovy.transform.PackageScope

class Numbers {

    // todo: package scope annotation is used to limit the scope to package only
//    @PackageScope
    int number

}